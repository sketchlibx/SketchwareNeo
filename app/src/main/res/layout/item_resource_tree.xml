<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:minHeight="44dp"
    android:paddingTop="3dp"
    android:paddingBottom="3dp"
    android:paddingEnd="4dp">

    <!--
      Tree connector lines.
      Width is driven by onMeasure() inside TreeLinesView (= depth × COLUMN_WIDTH).
      Height stretches top-to-bottom via top/bottom constraints.
    -->
    <mod.agus.jcoderz.editor.manage.resource.TreeLinesView
        android:id="@+id/treeLinesView"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!--
      Chevron icon.
      - INVISIBLE for file nodes — keeps horizontal layout stable (no margin jumps).
      - Rotates 0°↔90° on expand/collapse via ValueAnimator in adapter.
    -->
    <ImageView
        android:id="@+id/chevronIcon"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_mtrl_chevron_right_24"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/treeLinesView"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="?attr/colorOnSurfaceVariant"
        tools:ignore="ContentDescription" />

    <!--
      File / folder icon.
      Regular ImageView — Glide applies RoundedCorners transform for image files;
      vector/folder icons are loaded directly via setImageResource().
    -->
    <ImageView
        android:id="@+id/fileIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="4dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/chevronIcon"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_mtrl_folder" />

    <!--
      Filename label.
      Uses ellipsize=middle so both the start (path type) and extension stay visible.
    -->
    <TextView
        android:id="@+id/fileName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="2dp"
        android:ellipsize="middle"
        android:maxLines="1"
        android:textColor="?attr/colorOnSurface"
        android:textSize="13sp"
        android:lineSpacingMultiplier="1.0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/moreButton"
        app:layout_constraintStart_toEndOf="@id/fileIcon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="my_layout_file.xml" />

    <!--
      More (⋮) button.
      Visible in flat mode, GONE in tree mode (long-press used instead).
    -->
    <ImageButton
        android:id="@+id/moreButton"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="More options"
        android:padding="3dp"
        android:src="@drawable/ic_more_vert"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="?attr/colorOnSurfaceVariant" />

</androidx.constraintlayout.widget.ConstraintLayout>
